{
  "_comment": "References only — NEVER put actual secret values here. Maps env var names to Bitwarden items. field defaults to 'password'; 'username'/'notes'/'uri' read login fields; any other value is a custom field name. Loaded lazily by `load_secrets`.",
  "secrets": [
    { "env": "GEMINI_API_KEY",     "item": "GEMINI_API_KEY" },
    { "env": "OPENAI_API_KEY",     "item": "OPENAI_API_KEY" },
    { "env": "ANTHROPIC_API_KEY",  "item": "ANTHROPIC_API_KEY" },
    { "env": "OPENROUTER_API_KEY", "item": "OPENROUTER_API_KEY" },
  ]
}
